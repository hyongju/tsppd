


n = 5;
Q = zeros(n,n);
Q = sym(Q);
for i = 1:size(Q,1)
    Q(i,i) = sym('c');
end
for i = 1:size(Q,1)-1
    Q(i+1,i) = 0.5*sym('c');
    Q(i,i+1) = 0.5*sym('c');
end
Q(n,1) = 0.5*sym('c');
Q(1,n) = 0.5*sym('c');

Q(1:n-1,1:n-1) - Q(1:n-1,n)*(1/sym('c'))*Q(n,1:n-1)